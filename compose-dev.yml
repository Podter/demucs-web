services:
  redis:
    image: redis:8-alpine
    restart: unless-stopped
    environment:
      - REDIS_EXTRA_FLAGS=--save 60 1 --loglevel warning
    ports:
      - "6379:6379"
    volumes:
      - kv:/data

volumes:
  kv:

{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "NODE_ENV=production bun run typecheck && bun run scripts/build.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@elysiajs/static": "^1.2.0",
    "@t3-oss/env-core": "^0.12.0",
    "elysia": "^1.2.10",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.3"
  }
}
